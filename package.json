{
	"name": "electron-app",
	"version": "1.0.16",
	"description": "",
	"main": "main.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "electron .",
		"build": "electron-builder build --win --publish never",
		"deploy": "electron-builder build --win --publish always"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/Looky1173/Electron.git"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"electron": "^13.1.1",
		"electron-builder": "^22.10.5"
	},
	"dependencies": {
		"electron-is-dev": "^2.0.0",
		"electron-updater": "^4.3.9",
		"path": "^0.12.7"
	}
}
